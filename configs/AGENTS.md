# configs — Agent Guide

## Scope
Configuration templates (when present). Runtime configuration comes from env vars.

## Key invariants
- Never commit secrets.
- Env var contracts live in `libs/models/config.py`.

## References
- Root guide: `AGENTS.md`
