# tests/integration/fixtures — Agent Guide

## Scope
Fixture data and manifest templates for integration/E2E tests.

## Key invariants
- Template placeholders must be substituted at runtime.
- FileType enum values must match `libs/models/spatial.py`.
- Intent/type coherence must be maintained in fixtures.
